mixin header(isMain)
    - var mainMod = isMain ? 'header_main' : '';
    header(class=`header ${mainMod}`)
        +_header-top()
        +_header-bottom()
        .header__categories
            .container
                +categories()
        - if(isMain)
             .header__slider
                 +main-slider()
